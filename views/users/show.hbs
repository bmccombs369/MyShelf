<h3>{{user.username}}</h3>
<img src="{{user.photoUrl}}">
<p>Favorite Movie Quote: {{user.favoriteMovieQuote}}</p>
<ul>
    {{#each users.catalog}}
    <li>
        <p>{{catalog.name}}</p>
    </li>
    {{/each}}

    <form action="/users/{{homeworkAssignment._id}}?_method=DELETE" method="POST">
        <button type="submit">Delete User</button>
</ul>